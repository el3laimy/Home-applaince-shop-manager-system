# تقدم تنفيذ خطة الاعتمادية

تاريخ الدفعة الأولى: 9 سبتمبر 2026. الأساس `db2d39f`، والتعديلات الحالية محلية ولم تُنشر أو تُرفع إلى GitHub.

هذه دفعة إصلاح أولى، وليست إغلاقًا للخطة أو إصدارًا جاهزًا للعميل. النظامان المستهدفان Windows وLinux، وAndroid مرحلة مستقبلية.

## ما تم تنفيذه

| البند | التنفيذ الحالي | حدود الإغلاق |
|---|---|---|
| Q01 | اختبارات للسلوك الصحيح قبل إصلاح F1–F5؛ فشل الحالات محفوظ ثم نجاحها | اختبارات المخاطر الأخرى تُضاف مع مهامها |
| Q02 | ساعة قابلة للحقن لتواريخ شاشتي البيع والتقارير؛ Golden ثابتة دون تعديل الصور | تعميم الساعة على كل حالات الاستخدام واختبار تاريخين مستقلين ما زال مفتوحًا |
| Q03 / F7 | وظيفة CI للتحليل والاختبارات؛ بناء النظامين يعتمد على نجاحها؛ تشغيل على pull requests | لم يُشغّل GitHub Actions في هذه الدفعة؛ إثباته ينتظر الرفع |
| D01 / F1 | رفض الملف التالف والمخطط غير المطابق والجداول/الأعمدة الناقصة والعلاقات المكسورة قبل إغلاق القاعدة؛ فحص بعد النسخ؛ إبقاء نسخة رجوع بعد النجاح؛ فتح اتصال جديد والعودة للدخول | لا يزال الاستبدال نسخ ملفات؛ استئناف التعافي بعد انقطاع الجهاز، ومنع العمليات المتزامنة، وترقية نسخة قديمة عند الاسترجاع غير مكتملة |
| D02 / F2 | فشل النسخ التلقائي المعروف لا يمنع bootstrap؛ تنبيه في الواجهة؛ التعامل مع فشل استعراض المجلد؛ زوال التنبيه بعد نسخة ناجحة | جدولة كل 30 دقيقة وإعادة المحاولة عند عودة الوسيط غير منفذتين؛ الحالي ما زال نسخ startup يوميًا |
| A01 / F3 | تخصيص إجمالي الدفعات تراكميًا بدل مقارنة الدفعة الجديدة بكامل القسط، مع الاحتفاظ بتاريخ تسديد القسط السابق | لا ترحيل تلقائي لحالات قديمة دون دفعة جديدة؛ فحص البيانات التاريخية A08 مفتوح |
| A02 / F4 | رفض تكرار المنتج داخل مدخلات البيع والشراء قبل الكتابة | السلة الحالية تجمع المنتج؛ اختبار الطبقة الداخلية يثبت الرفض دون تغيير المخزون أو الفواتير |
| A03 / F5 | توزيع صافي الفاتورة بين السطور بالأعداد الصحيحة؛ رد تراكمي للكميات مع خصم المردود سابقًا؛ حفظ فرق القرش في مجموعات أسعار وحدة؛ معاينة تستخدم الحساب نفسه | لا يصحح مبالغ رُدت فعليًا في بيانات قديمة؛ لا تغيير تلقائي للدفتر التاريخي |

تخزين مرتجع بوحدات سعر مختلفة يسمح بحفظ مبلغ مثل 299.99 لثلاث وحدات دون تغيير المخطط: سجلان يختلف سعر الوحدة بينهما بقرش، مع بقاء مجموع الكمية والتكلفة والمبلغ صحيحًا. المرتجع الذي نصيبه النقدي صفر بسبب التقريب لا ينشئ قيد مبيعات صفريًا؛ حركة المخزون وتكلفته تبقيان مسجلتين.

لم تتغير schemaVersion ولم تُحذف اختبارات أصلية ولم تُحدّث صور Golden. أصبحت `sqlite3` اعتمادية تشغيل مباشرة للفحص، بنفس النسخة الموجودة 3.3.3؛ لم تُرقَّ بقية الحزم.

## التحقق

- قبل الإصلاح: اختبارات الحالات الأربع الأولى فشلت؛ اختبار المرتجع توقع 29999 ووجد 30000.
- بعد أول مجموعة إصلاحات: 72 اختبارًا نجحت، بينها Golden الأصلية.
- اختبارات التقريب الموسعة: 6 نجحت، تشمل دفعات مرتجع 1+1+1 و2+1، مبلغًا أقل من قرش للوحدة، وأصنافًا متعددة بترتيب إرجاع عكسي.
- التحليل الساكن بعد إصلاح التقريب: `No issues found!`.
- التحقق النهائي: **78 اختبارًا نجحت، بلا فشل**، خلال نحو 106 ثوانٍ. يشمل ذلك 64 اختبارًا أصليًا و14 اختبار انحدار جديدًا.
- بناء Linux النهائي: نجح بالأمر `flutter build linux --release --no-pub`، والناتج `Frontend/alikhlas_pos/build/linux/x64/release/bundle/alikhlas_pos`. لم يُنفذ اختبار Windows أو تشغيل أجهزة الطباعة في هذه الدفعة.
- `git diff --check`: نجح. لم يتم نشر الحزمة أو ادعاء قبول الأجهزة الفعلية.

الأدلة تحفظ في `reports/2026-09-09/implementation-batch-01/`. ملفات التقرير القديم تمثل حالة ما قبل الإصلاح وتبقى محفوظة للمقارنة.

## المتبقي بالترتيب

1. إكمال D01: استبدال قابل للتعافي بعد انقطاع العملية/الجهاز، إدارة حالة استرجاع واضحة، منع الكتابات أثناء العملية، واختبار اتصال الواجهة بعد الاسترجاع على جهاز فعلي.
2. D02–D03: نسخ أثناء اليوم وإعادة محاولة، حزمة الصور والقاعدة، ومصفوفة توافق واسترجاع المخططات القديمة.
3. A04–A08: منع النقر المكرر وإعادة المحاولة المكررة، فصل حفظ الفاتورة عن فشل الطباعة، فحوص اتساق أوسع، والتعامل المنضبط مع بيانات تاريخية متأثرة.
4. U01–U05: سجل كامل للفواتير، ربط رد فائض التقسيط بالواجهة، أول أرصدة وجرد وتصحيح، رسائل مبسطة ومسودات.
5. I01–I05: مُثبّتا Windows وLinux، إعداد أول تشغيل، استعادة الوصول، تحديث آمن ودليل ودعم.
6. M5: اختبارات أجهزة فعلية ومستخدمين وطباعة ونقل بيانات؛ لا تُستبدل باختبارات Flutter.

إثبات توقيع وتثبيت Windows واختيار توزيعة Linux وطرازات الطابعات يحتاج بيئات وأجهزة التسليم في مرحلته؛ لا يوقف الإصلاحات المحلية الحالية.


## دفعة المتابعة — النسخ أثناء التشغيل

- نُفذ D02 جزئيًا إضافيًا: فحص الاستحقاق كل دقيقة ونسخة كل 30 دقيقة، ووقت آخر نجاح مستمر عبر إعادة التشغيل.
- فشل النسخ لا يحدّث وقت النجاح، ويُعاد المحاولة عند رجوع الوجهة. منع تداخل فحوص النسخ التلقائي، وإلغاء المؤقت عند التخلص من نطاق التطبيق.
- تمت إضافة ساعة قابلة للحقن لحساب موعد النسخ واختبارات مرور 29/30 دقيقة، إعادة إنشاء use-case، عودة الوجهة والطلبات المتزامنة، واختبار دورة حياة المؤقت.
- المجموعة الشاملة قبل إضافة نقطة حقن فحص المؤقت: 81 اختبارًا ناجحًا. اختبار المؤقت المستقل نجح بعد ذلك؛ الفحص المركز النهائي والتحليل والبناء محفوظة في أدلة الدفعة الثانية عند انتهائها.
- لا تزال النسخة تحتوي قاعدة البيانات فقط، وتعمل كل 30 دقيقة حتى إن لم تتغير بيانات المحل؛ تحسين كشف التغييرات وحزمة الصور والعمل عند إغلاق اليومية ما زال مفتوحًا. التعافي من انقطاع الاسترجاع ومنع التزامن مع كل الكتابات لم يُغلقا.
- الأدلة: `reports/2026-09-09/implementation-batch-02/`؛ لا يوجد نشر أو تشغيل Windows أو إثبات طباعة جديد.
- نتيجة المتابعة النهائية: التحليل نظيف، واختبارات النسخ والمؤقت الأربعة ناجحة، وبناء Linux النهائي نجح. انتهت جلسة البناء 7274 بالرمز 0، وحُفظ سجلها في أدلة الدفعة الثانية.


## دفعة ثالثة — التعافي من استرجاع غير مكتمل

- أضيفت خدمة `RestoreRecovery`، وتُستدعى قبل فتح قاعدة التشغيل الافتراضية. علامة ثابتة تشير إلى استرجاع لم يكتمل، وتحتوي بصمة نسخة رجوع ثابتة المسار بجوار القاعدة.
- الاسترجاع يثبت نسخة مرشحة ويفحصها، وينشئ نسخة رجوع متسقة بـSQLite VACUUM INTO، ويغلق الاتصال قبل وضع العلامة والاستبدال. فشل النسخ أو الفحص يستدعي التعافي؛ نجاح الفحص وتفريغ الكتابة يزيل العلامة.
- إذا توقف التطبيق أثناء الاستبدال، يُعاد ملف الرجوع عند الفتح التالي. التعافي نفسه قابل لإعادة المحاولة إذا قوطع؛ لا يحذف نسخة الرجوع. إذا تلفت نسخة الرجوع أو تغيرت بصمتها يبقى الأصل/الملفات والعلامة ولا يُفتح ملف ناقص كقاعدة جديدة.
- أضيف منع تداخل النسخ اليدوي والتلقائي مع الاسترجاع داخل نفس use-case، وحوار انتظار غير قابل للإغلاق يمنع تفاعل المستخدم أثناء الاسترجاع. قفل العمليات المالية الجارية مسبقًا أو تشغيل عدة عمليات نظام ما زال ضمن D04/A04، ولا نعده مغلقًا بهذه الحماية.
- أضيفت 3 اختبارات: ملف تشغيل ناقص، ملف تشغيل مفقود، ونسخة رجوع تغيرت. اختبارات الاسترجاع والعمليات المستهدفة مرت (40). تم تكييف محاكاة فشل نسخ الاسترجاع لتصيب نسخ الملف المرشح إلى ملف التشغيل، مع بقاء تأكيد استعادة البيانات الأصلية.
- التحليل النهائي نظيف. المجموعة الشاملة نجحت: 85 اختبارًا بلا فشل خلال 76 ثانية. بناء Linux النهائي نجح وانتهى بالرمز 0؛ سجله محفوظ في أدلة الدفعة الثالثة. لم يُنفذ تشغيل جهاز عميل أو بناء Windows في هذه الدفعة.
- حدود الدليل: محاكاة لحالات الملفات بعد توقف العملية، وليست اختبار قطع كهرباء على جهاز العميل أو ضمانًا لدوام بيانات metadata على جميع أنظمة الملفات. استرجاع مخطط قديم وحزمة الصور وقفل تشغيل نسختين غير مكتملة بعد.
- الأدلة: `reports/2026-09-09/implementation-batch-03/`.


## دفعة رابعة — منع تشغيل عمليتين على بيانات المحل

- أضيف `ApplicationLock` قبل التعافي وفتح قاعدة التشغيل الافتراضية. يمسك ملف قفل منفصلًا بعمر العملية ويستمر عبر إغلاق اتصال القاعدة للاسترجاع وإعادة فتحه.
- ملف القفل لا يُحذف عند التشغيل أو الإغلاق؛ نظام التشغيل يحرر القفل بعد انتهاء العملية، ووجود الملف وحده لا يعني وجود قفل نشط. رفض النسخة الثانية يعرض رسالة عربية تطلب استخدام النافذة الحالية.
- اختبار عمليات مستقلة على Linux: مالك يمسك القفل، منافس يخرج بالرمز 23، ثم إنهاء المالك بالقوة وتشغيل خليفة يحصل على القفل. أداة إعادة التنفيذ: `python3 tool/check_application_lock.py /path/to/dart` من مجلد التطبيق.
- القفل استشاري على Linux؛ لا يمنع أداة خارجية لا تتبع هذا البروتوكول. لا يعوض تسلسل المعاملات داخل العملية، ولا يُدعى هنا دعم قاعدة مشتركة على الشبكة أو بين أجهزة. مصدر سلوك الأقفال: https://api.dart.dev/dart-io/RandomAccessFile/lock.html
- لا يوجد إثبات تشغيل Windows للقفل بعد؛ الاختبار الفعلي المحلي خاص بـLinux. التحليل الساكن الأولي نظيف؛ الاختبارات الشاملة والبناء جاريان ويثبت ناتجهما عند الانتهاء.
- الأدلة: `reports/2026-09-09/implementation-batch-04/`.
- نتيجة الدفعة الرابعة: 85 اختبارًا بالمجموعة الشاملة ناجحة، واختبار رسالة التشغيل المكرر الجديد ناجح مستقلًا، والتحليل النهائي (lib/test/tool) نظيف، وبناء Linux النهائي نجح. اختبار القفل بين عمليات مستقلة نجح بعد آخر تعديل أيضًا.
- أضيف اختبار العمليات إلى CI على Linux وWindows باستخدام Dart SDK الفعلي. هذه إضافة workflow محلية؛ تشغيل CI على Windows لم يُثبت بعد ولم يحصل رفع للمستودع.

## دفعة خامسة — الوصول للفواتير القديمة ورد فائض التقسيط

- نُفذ جزء من U02: البحث في جميع فواتير البيع بالرقم أو اسم العميل أو الهاتف، مع 20 نتيجة في الصفحة والتنقل السابق/التالي؛ لم تعد شاشة المرتجعات تعتمد على آخر 8 مبيعات. البحث يتعامل مع `%` كنص عادي، والترتيب بالتاريخ ثم المعرّف لتجنب غموض تساوي التوقيت.
- معاينة المرتجع تجلب المديونية الحالية من خطة الأقساط بعد التحصيل، وتعرض «خصم من المديونية» و«فائض يُرد للعميل». خيار الخصم لا يظهر لفاتورة دون خطة. عند وجود فائض يلزم اختيار كاش أو محفظة قبل التسجيل، وتُمرر الطريقة لمنطق الترحيل الموجود دون تعديل قواعد القيود أو المخطط.
- منع فتح عمليتي مرتجع متزامنتين من الشاشة نفسها، وإظهار رسالة عند استنفاد الكمية القابلة للمرتجع. هذه حماية واجهة محدودة وليست إغلاقًا لبند idempotency لجميع العمليات.
- أضيف اختبار استعلامات لسجل 31 فاتورة والبحث برقم قديم/عميل/هاتف وصفحات بلا تداخل. أضيف اختبار واجهة كامل يجد فاتورة خارج الصفحة الأولى، يبحث عنها باسم العميل، يمنع التسجيل دون طريقة للفائض، ثم يسجل رد الفائض بالمحفظة ويتحقق من المخزون والمديونية. الاختبار كشف خطأ تحديث حالة عند تغيير الصفحة وأُصلح قبل التحقق الشامل.
- توسع اختبار التقسيط القائم بتأكيد أن المعاينة تقرأ مديونية 2000 بعد تحصيل 8000، حتى إذا كان حقل المتبقي التاريخي في الفاتورة 10000. سيناريو رفض فائض كاش دون يومية مفتوحة باقٍ ضمن الاختبار الأصلي.
- الاختبارات المركزة ناجحة والتحليل نظيف. التحقق الشامل وبناء Linux تسجل نتائجهما بعد انتهائهما في الأدلة أدناه.
- U02 ما زال مفتوحًا جزئيًا: فلتر التاريخ، تفاصيل الفاتورة والمدفوع والمرتجع، وإعادة الطباعة من السجل لم تنفذ في هذه الدفعة. اختبار أجهزة Windows والطابعات وتسليم مُثبّت للمستخدم النهائي لم يُنفذ.
- الأدلة: `reports/2026-09-09/implementation-batch-05/`.
- نتيجة التحقق الشامل للدفعة الخامسة: **88 اختبارًا ناجحًا بلا فشل** خلال 87 ثانية، بما فيها اختبارات Golden الأصلية دون تغيير صورها. التحليل `dart analyze lib test tool` نظيف، و`git diff --check` نجح.
- بناء Linux النهائي نجح بالأمر `flutter build linux --release --no-pub` وانتهى بالرمز 0. الناتج `Frontend/alikhlas_pos/build/linux/x64/release/bundle/alikhlas_pos`؛ هذا دليل بناء، لا اختبار تثبيت أو قبول أجهزة المستخدمين.

## دفعة سادسة — ترشيح التاريخ وتفاصيل الفاتورة وإعادة الطباعة — 2026-09-10

- أضيف نطاق تاريخ إلى البحث في سجل الفواتير؛ البداية شاملة ونهاية اليوم الأخير تُنفذ كحد حصري عند بداية اليوم التالي. يجتمع البحث النصي مع النطاق، وتعود الصفحة للأولى عند تغيير الفترة أو مسحها.
- اختيار الفترة معرّب باستخدام حزمة `flutter_localizations` من SDK محليًا، ضمن نافذة التاريخ. لم تُرقَّ حزم الطرف الثالث.
- من «تفاصيل وطباعة» يمكن مراجعة الفاتورة وإنشاء المرتجع. تعرض التفاصيل تحصيل الأقساط بعد البيع وإجمالي المرتجع والمديونية الحالية بشكل منفصل عن أرقام الفاتورة الأصلية. إعادة الطباعة تستخدم المستند المحفوظ وأرقامه الأصلية، مع توضيح هذا الفرق للمستخدم.
- أضيفت نقطة حقن لخدمة طباعة الفاتورة لاختبار فشلها وإعادة المحاولة. يظهر الفشل داخل نافذة الفاتورة، ويُمنع تداخل طلبات الطباعة. لا تنشئ إعادة الطباعة بيعًا جديدًا.
- الاختبارات المركزة الثلاثة نجحت: حدود اليوم والبحث مع التاريخ؛ صفحات البحث القديم؛ ومسار واجهة يشمل إدخال تاريخ عربي، نتيجة فارغة، مسح الترشيح، تفاصيل الفاتورة، فشل الطابعة ثم نجاح إعادة المحاولة، وإنشاء مرتجع بفائض محفظة دون زيادة عدد فواتير البيع.
- التحقق الشامل والبناء قيد التنفيذ. الأدلة: `reports/2026-09-10/implementation-batch-06/`.
- لا تزال الطباعة على جهاز فعلي وWindows وقبول المستخدمين غير مثبتة. كما يبقى تعميم منع تكرار العمليات المالية واستكمال حماية الاسترجاع وحزمة الصور والمثبتات ضمن الخطة؛ هذه الدفعة لا تغلق جاهزية الإصدار.
- نتيجة التحقق الشامل: **89 اختبارًا ناجحًا بلا فشل** خلال 85 ثانية، بما فيها Golden الأصلية دون تعديل صورها. التحليل `dart analyze lib test tool` نظيف، و`git diff --check` نجح.
- بناء Linux النهائي نجح بالأمر `flutter build linux --release --no-pub` وانتهى بالرمز 0؛ السجل محفوظ في أدلة الدفعة السادسة. الناتج `Frontend/alikhlas_pos/build/linux/x64/release/bundle/alikhlas_pos`. لم يُنفذ تثبيت على جهاز عميل أو طباعة فعلية بهذه الدفعة.

## دفعة سابعة — منع تكرار البيع وفصل الحفظ عن تحميل الإيصال — 2026-09-10

- أُثبت الخلل أولًا باختبار إرسال استدعاء زر البيع مرتين قبل تحديث الواجهة: توقع فاتورة واحدة فوجد فاتورتين.
- أضيف معرّف عشوائي للعملية في واجهة البيع، وبصمة تشمل العميل والأصناف والمدفوعات والخصم وشروط التقسيط. يُحفظ رقم الفاتورة والبصمة في مساحة `operation.sale.*` داخل مخزن المفاتيح الفريد `app_settings`، ضمن نفس معاملة قاعدة البيانات التي تحفظ البيع. لا تغيير للمخطط ولا ترقية لقواعد القيود.
- نفس المعرّف ونفس البيانات يعيدان رقم الفاتورة دون قيد أو خصم مخزون جديد؛ تغيير بيانات عملية محفوظة يُرفض. فشل الأعمال المعروف لا يحجز المعرّف، ويمكن تصحيح الطلب وإرساله. سجلات العمليات لا تُحذف بسياسة تنظيف الإعدادات.
- الواجهة تمنع الإرسال المتكرر وتعديل السلة أثناء الحفظ. عند خطأ غير متوقع تحتفظ بالطلب نفسه وبمعرّفه وتعرض إعادة محاولة للتحقق من نفس البيع. تُفرغ السلة فور تأكيد النجاح قبل تحميل الإيصال، ويظهر فشل تحميل الإيصال كفشل عرض لفاتورة محفوظة.
- الاختبارات المركزة نجحت للنقر المكرر وفشل تحميل الإيصال وفقد الرد بعد نجاح الحفظ. اختبار قاعدة ملفية أكد الطلبات المتزامنة وإعادة فتح القاعدة بنفس المفتاح ورفض تغيير البيانات وعدم زيادة الفواتير/القيود أو خصم المخزون مرة أخرى.
- حدود A04 المتبقية: الحماية الحالية تخص مسار البيع بهذا المعرّف؛ الشراء والتحصيل والمرتجعات تحتاج تعميمها. الطلب المعلّق في الواجهة لم يصبح مسودة مستمرة عبر إغلاق التطبيق أو مغادرة شاشة البيع؛ إعادة تشغيل اختبار القاعدة تثبت حفظ نتيجة المعرّف ولا تثبت استعادة مسودة واجهة المستخدم. البند ما زال قيد التنفيذ.
- الاختبارات الشاملة والتحليل والبناء تسجل نتائجها عند الانتهاء. الأدلة: `reports/2026-09-10/implementation-batch-07/`.
- التحقق الشامل: **93 اختبارًا ناجحًا بلا فشل** خلال 97 ثانية، مع بقاء Golden الأصلية دون تحديث. أُعيد اختبار قاعدة الملف بعد إضافة SQLite trigger يتعمد إفشال كتابة سجل العملية؛ أكد رجوع الفاتورة والقيود والمخزون معًا ونجح. سجله `atomicity.log`.
- التحليل النهائي `dart analyze lib test tool` نظيف، و`git diff --check` نجح. بناء Linux النهائي نجح بالأمر `flutter build linux --release --no-pub` وانتهى بالرمز 0؛ سجله في أدلة الدفعة السابعة. لا يوجد إثبات تثبيت Windows أو طابعة فعلية جديد.

## دفعة ثامنة — طلب بيع مستمر عبر مغادرة الشاشة وإعادة الفتح — 2026-09-10

- أضيف `PendingSale` يحتفظ بمعرّف العملية والعميل والأصناف والأسعار والمدفوعات وملاحظاتها والخصم وشروط التقسيط في `pending.sale.v1` داخل قاعدة البيانات قبل إرسال البيع. لا يستبدل طلب جديد طلبًا لم تُحسم نتيجته.
- عند فتح شاشة البيع، تُقرأ الحالة المخزنة أولًا قبل السماح ببيع جديد. يظهر الطلب السابق للتحقق وإعادة المحاولة بنفس المعرّف؛ لا يجري ترحيله تلقائيًا دون إجراء المستخدم. فشل قراءة الحالة أو إصدار غير معروف يمنع بدء بيع جديد ويحافظ على البيانات.
- الإقرار بإتمام الطلب لا يُزيله إلا عند وجود نتيجة عملية محفوظة بالمفتاح نفسه. الإلغاء متاح للطلب غير المحفوظ فقط؛ فحص الإلغاء والحذف، وفحص الطلب الفعّال وتسجيل البيع، يعملان في معاملات لمنع سباق الإلغاء مع الحفظ.
- اختبار قاعدة ملفية نجح مع إعادة الفتح قبل تسجيل الفاتورة وبعد تسجيلها وقبل إقرار الواجهة؛ بقيت فاتورة واحدة والمخزون صحيحًا. شمل رفض إلغاء طلب محفوظ، وعدم قبول إقرار بمفتاح مختلف، ورفض إرسال طلب أُلغي، والمحافظة على حالة ذات إصدار غير معروف.
- اختبار الواجهة نجح بعد فقد الرد عقب الحفظ: مغادرة البيع والعودة، التخلص من نطاق التطبيق وإعادة إنشائه وتسجيل الدخول، ظهور نفس الطلب، رفض إلغائه لأنه محفوظ، ثم استكماله دون تكرار الفاتورة. هذا اختبار دورة حياة Flutter مع اختبار قاعدة ملفية منفصل؛ ليس اختبار قطع كهرباء أو قتل عملية واجهة أصلية على جهاز العميل.
- أضيف اختبار حفظ شروط التقسيط والقيم الدقيقة وملاحظات الدفع، واختبار تزامن الإلغاء والإرسال. التحقق النهائي للدفعة وأدلته في `reports/2026-09-10/implementation-batch-08/`.
- لا تزال حماية الشراء والتحصيل والمرتجعات، وحماية الاسترجاع من كل العمليات الجارية، ومُثبتات Windows/Linux وقبول الأجهزة الفعلية مفتوحة؛ لا يُغلق A04 أو هدف الإصدار بهذه الدفعة.
- نتيجة التحقق قبل حارس الطلب السابق الذي يظهر أثناء فتح الشاشة: 95 اختبارًا ناجحًا خلال 95 ثانية؛ وبعد إضافة ملاحظات الدفع نجحت الاختبارات المركزة الخمسة، والتحليل نظيف، والبناء الأول نجح. أضيف بعد ذلك حارس يمنع تنفيذ طلب أقدم تلقائيًا إذا ظهر أثناء بقاء شاشة بيع جديدة مفتوحة؛ يعرض الطلب للمراجعة وينتظر تأكيد المستخدم. الاختبار الجديد يثبت عدم تسجيل أي فاتورة قبل هذا التأكيد. نتائج النسخة النهائية تثبت أدناه بعد انتهاء إعادة التحقق.
- التحقق الشامل للنسخة النهائية بعد حارس الطلب السابق: **96 اختبارًا ناجحًا بلا فشل** خلال 135 ثانية. التحليل النهائي نظيف. السجل النهائي `tests-final.log` محفوظ بعد انتهاء العملية بالرمز 0؛ النسخة الأقدم من السجل تبقى للمقارنة. بناء النسخة النهائية تسجل نتيجته عند الانتهاء.
- بناء Linux النهائي نجح وانتهى بالرمز 0؛ سجله `linux-build-final.log`. لم يُختبر إيقاف عملية واجهة أصلية أو تثبيت Windows/الطابعات على جهاز المستخدم في هذه الدفعة.

## دفعة تاسعة — منع تكرار الشراء وفصل طباعة الباركود — 2026-09-10

- أُثبت تكرار الشراء باختبار إرسال استدعاء الزر مرتين قبل تحديث الواجهة، مع رصيد محاسبي كافٍ: ظهرت فاتورتان بدل واحدة. جُهز الرصيد بحركة بيع فعلية في بيانات الاختبار؛ رصيد فتح اليومية وحده ليس تمويلًا لحساب الخزينة.
- أضيف معرّف عملية وبصمة للمورد والأصناف والكميات والتكاليف والمدفوعات إلى `createPurchase`. يُحفظ سجل `operation.purchase.*` مع الشراء في المعاملة نفسها. الطلب المتكرر يعيد رقم الفاتورة، وتغيير بيانات معرّف محفوظ يُرفض دون حركة إضافية.
- طلب الموافقة على الرصيد السالب لا يحفظ نتيجة عملية؛ يمكن استكماله بنفس المعرّف بعد موافقة المستخدم. إعادة طلب سبق حفظه تعيد نتيجته حتى إن كان طلب الإعادة لا يحمل موافقة جديدة؛ لا تنشئ مصروفًا أو مخزونًا جديدًا.
- واجهة الشراء تمنع تكرار الإرسال وتعديل السلة أثناء الحفظ. عند خطأ غير متوقع تُبقي الطلب نفسه ومفتاحه لإعادة المحاولة. بعد نجاح الحفظ تُفرَّغ السلة قبل سؤال/تنفيذ طباعة الباركود، وتوضح رسالة فشل الطابعة أن الشراء محفوظ وأن إعادة طباعة الباركود متاحة من المخزون.
- اختبارات الواجهة أثبتت فاتورة واحدة للنقر المكرر وفقد الرد بعد commit، وفشل طباعة الباركود مع بقاء السلة فارغة والمخزون صحيحًا. اختبار قاعدة ملفية أكد الطلبات المتزامنة وإعادة الفتح ورفض تغيير الطلب، وحقن فشل في حفظ معرّف العملية مع رجوع الفاتورة والمخزون والقيود معًا.
- حدود الدفعة: طلب الشراء المعلّق في الواجهة لم يُحفظ بعد كمسودة تستعاد عند مغادرة الشاشة أو إغلاق التطبيق؛ الحماية الدائمة الحالية تعيد نتيجة نفس المعرّف عند توافره. يلزم استكمال ذلك مثل البيع، وتعميم الحماية على التحصيل والمرتجعات. لا يُغلق A04 بهذه الدفعة.
- الأدلة ونتائج التحقق الشامل والبناء: `reports/2026-09-10/implementation-batch-09/`.
- نتيجة التحقق الشامل: **100 اختبار ناجح بلا فشل** خلال 81 ثانية، مع بقاء Golden الأصلية دون تعديل صورها. التحليل `dart analyze lib test tool` نظيف و`git diff --check` نجح. سجل الاختبارات محفوظ بعد انتهاء العملية بالرمز 0. بناء Linux لم يُثبت بعد عند كتابة هذا السطر، وتضاف نتيجته عند الانتهاء.
- بناء Linux النهائي نجح وانتهى بالرمز 0 بعد إعادة محاولة واحدة إثر انتهاء مهلة المراجعة الآلية قبل بدء المحاولة الأولى. السجل `linux-build.log` محفوظ. لا يوجد اختبار طابعة فعلية أو تشغيل Windows جديد في هذه الدفعة.

## دفعة عاشرة — استعادة طلب الشراء المعلّق — 2026-09-10

- أضيف `PendingPurchase` لحفظ معرّف الطلب والمورد والأصناف والكميات والتكاليف والمدفوعات وملاحظاتها قبل الإرسال في `pending.purchase.v1`. يُقرأ عند فتح شاشة الشراء، ولا يستبدل طلب جديد طلبًا سابقًا غير محسوم.
- موافقة الرصيد السالب لا تُخزّن في الطلب. الاستعادة لطلب لم يُحفظ بعد تستدعي الفحص والموافقة مجددًا؛ أما طلب سبق حفظ فاتورته فيعيد النتيجة نفسها دون تنفيذ مالي جديد.
- الإقرار لا يمسح الطلب إلا بعد وجود نتيجة عملية محفوظة؛ الإلغاء يرفض الطلب المحفوظ، وفحص الإلغاء والحذف والإرسال تعمل في معاملات. إذا ظهر طلب سابق أثناء فتح شاشة جديدة، يُعرض للمراجعة بدل تنفيذه تلقائيًا.
- بعد الاستعادة تُجهّز ملصقات الباركود من كميات الطلب المحفوظ ومنتجات المخزون، مع بقاء فصل فشل الطباعة عن نجاح الشراء. رفض الموافقة أثناء إدخال شراء جديد يعيد المستخدم لمدخلاته الحالية؛ الطلب المستعاد يبقى متاحًا للتحقق أو الإلغاء الصريح إذا لم يُحفظ.
- الاختبارات المركزة الخمسة نجحت: إعادة فتح قاعدة ملفية قبل/بعد commit، ضرورة الموافقة بعد الاستعادة، ثبات رقم الفاتورة والمخزون، رفض إقرار بمفتاح آخر أو إلغاء فاتورة محفوظة، إلغاء طلب غير محفوظ، سباق الإلغاء/الإرسال، وإصدار حالة غير معروف. اختبار الواجهة يشمل التنقل وإعادة إنشاء نطاق التطبيق وتسجيل الدخول وإعادة نفس الطلب، مع عدم تكرار الفاتورة.
- هذا إثبات محلي للكود ودورة حياة Flutter وقاعدة ملفية، ولا يعوض اختبار إيقاف عملية الواجهة الأصلية/انقطاع الكهرباء أو Windows والطابعات الفعلية. حماية التحصيل والمرتجعات وبقية الخطة ما زالت مفتوحة.
- الأدلة ونتائج الفحص الشامل والبناء تحفظ في `reports/2026-09-10/implementation-batch-10/`.
- التحقق الشامل النهائي: **102 اختبار ناجح بلا فشل** خلال 95 ثانية؛ التحليل `dart analyze lib test tool` نظيف و`git diff --check` نجح. بقيت صور Golden الأصلية دون تحديث. حفظ سجل الاختبارات بعد انتهاء العملية بالرمز 0. نتيجة البناء تضاف عند الانتهاء.
- بناء Linux النهائي نجح؛ يؤكد السجل `reports/2026-09-10/implementation-batch-10/linux-build.log` إنتاج `build/linux/x64/release/bundle/alikhlas_pos`. هذا إثبات بناء، وتظل اختبارات التثبيت على جهاز المستخدم وWindows والطابعات الفعلية مفتوحة.

## دفعة حادية عشرة — منع تكرار واستعادة بقية الحركات المالية — 2026-09-10

- عُمّم إيصال العملية الدائم على تحصيل أقساط العملاء، سداد أقساط الموردين، مرتجع البيع، المصروف، وفتح/إغلاق الوردية. يحمل كل طلب معرّفًا وبصمة بيانات؛ تكرار المعرف والبيانات يعيد النتيجة الأولى فقط، وتغيير البيانات يرفض قبل إنشاء قيد أو تعديل رصيد أو مخزون.
- أضيف طلب مالي معلّق واحد يحفظ قبل التنفيذ ويستعاد بعد إغلاق التطبيق. عند عودة المستخدم تظهر نافذة عربية تشرح نوع العملية وتتيح التحقق من **نفس** الطلب أو إلغاءه فقط إن لم يكن قد حُفظ. لا تنفذ النافذة الحركة تلقائيًا، ولا تسمح بإلغاء طلب له إيصال عملية محفوظ.
- لا تُخزَّن موافقة الرصيد السالب. يعرض الطلب المستعاد التحذير ويطلب موافقة جديدة، وأي إلغاء للتحذير يزيل الطلب غير المحفوظ كي لا يحجب عملية لاحقة.
- غُطيت جميع نقاط الواجهة: شاشة الأقساط، التحصيل السريع من كشف العميل/المورد، المرتجعات، المصروفات، وفتح/إغلاق اليومية. حارس الواجهة يمنع تداخل محاولات تحصيل أو وردية، والإيصال الدائم يحمي أيضًا من فقد الرد بعد commit.
- الاختبارات الجديدة تتحقق من التوازي، إعادة فتح قاعدة ملفية بعد فقد الرد وقبل الإقرار، رفض تغيير الحمولة، عدم إمكان إلغاء طلب تم حفظه، الإلغاء الآمن لطلب غير محفوظ، وواجهة الاستعادة بعد تسجيل الدخول. لم تُعدّل اختبارات سابقة أو صور Golden.
- التحقق النهائي: **108 اختبارات ناجحة** بلا فشل خلال 87 ثانية؛ `dart analyze lib/v2 lib/main.dart test/v2` نظيف، و`git diff --check` نجح. بناء Linux بالأمر `flutter build linux --release --no-pub` نجح وأنتج `build/linux/x64/release/bundle/alikhlas_pos`.
- هذه أدلة محلية لقاعدة البيانات ودورة Flutter. ما يزال اختبار إيقاف عملية سطح المكتب الحقيقي/انقطاع الكهرباء، وWindows والطابعات الفعلية، والمثبتات والتحديث وقبول المستخدمين خارج هذه الدفعة.

## دفعة ثانية عشرة — فحص اتساق السجل والمخزون والذمم — 2026-09-11

- أضيف فحص اتساق للقراءة فقط داخل شاشة النسخ الاحتياطي. يفحص توازن القيود ومراجعها، تسلسل حركات المخزون ورصيده، بنود ومبالغ فواتير البيع والشراء ومدفوعاتها، المرتجعات، جداول الأقساط وحالاتها، وتطابق المتبقي في الأقساط مع ذمم العملاء والموردين. يعرض التقرير العربي المشاكل التي يعثر عليها ولا يعدّل أي بيانات تلقائيًا.
- أصلح إنشاء عملية شراء آجل جديدة ليولد جدول قسط المورد المطلوب بدل إنشاء خطة بلا دفعات. كما تسجل كمية المخزون الافتتاحية لحساب جديد حركة `opening_stock` متسقة مع رصيد الصنف. البيانات القديمة غير المتسقة تُبلغ للمستخدم في الفحص ولا تُغيّر بصمت.
- أضيفت اختبارات لحالة تشغيل كاملة سليمة، واكتشاف قيد غير متزن ومرجع قيد ورصيد مخزون متعارض دون إصلاحه، وظهور أداة الفحص في واجهة النسخ الاحتياطي. عُزز اختبار المورد السابق ليتحقق من جدول القسط ومبلغه وحالته بدل افتراض غيابه.
- التحقق النهائي: **112 اختبارًا ناجحًا** بلا فشل؛ `dart analyze lib/v2 lib/main.dart test/v2` نظيف، و`flutter build linux --release --no-pub` نجح وأنتج `build/linux/x64/release/bundle/alikhlas_pos`.
- الفحص ليس أداة إصلاح ولا دليلًا على سلامة ملفات تاريخية لم تُشغّل عليه. يبقى اختبار التثبيت على Windows، والنسخ/الاستعادة بالصور، وانقطاع الكهرباء الحقيقي، والطابعات وقبول المستخدمين ضمن خطة الإصدار المفتوحة.
- الأدلة: `reports/2026-09-11/implementation-batch-12/`.

## دفعة ثالثة عشرة — حاجز الكتابة أثناء الاستعادة — 2026-09-11

- أضيف `V2WriteBarrier` لطبقة الاستخدام. عند بدء الاستعادة يمنع قبول أي عملية كتابة جديدة، وينتظر كل كتابة سبق قبولها حتى تنتهي، ثم يمنح الاستعادة وصولًا حصريًا قبل إغلاق اتصال SQLite واستبدال الملف.
- أصبحت معاملات البيع والشراء والأقساط والمرتجعات والمصروفات، الطلبات المعلّقة وإيصالات منع التكرار، بيانات الأصناف والعملاء والموردين، إعدادات المحل، الحسابات والوردية تمر عبر الحاجز. بعد إغلاق القاعدة يرفض نفس نطاق التطبيق أي كتابة برسالة تطلب إعادة فتحه.
- أضيف اختبار يحجز كتابة جارية، يطلب الاستعادة، ويتحقق من أنها لا تغلق القاعدة مبكرًا وأن إنشاء عميل جديد يرفض أثناء الانتظار. بعد تحرير الكتابة تستكمل الاستعادة. اختبارات الاستعادة السابقة بقيت ناجحة.
- التحقق النهائي: **113 اختبارًا ناجحًا** بلا فشل؛ `dart analyze lib/v2 lib/main.dart test/v2` نظيف، و`flutter build linux --release --no-pub` نجح وأنتج `build/linux/x64/release/bundle/alikhlas_pos`.
- الحاجز داخل عمليات v2 للتطبيق ولا يحول دون تعديل ملف قاعدة البيانات مباشرةً من أداة خارجية. لا يزال اختبار قطع الكهرباء أو قتل عملية سطح المكتب الحقيقية، واستعادة الصور وحزمة النسخ القابلة للنقل، وWindows والطابعة الفعلية مفتوحًا.
- الأدلة: `reports/2026-09-11/implementation-batch-13/`.

## دفعة رابعة عشرة — حزمة نسخ قابلة للنقل تشمل الصور — 2026-09-11

- أضيف زر «حزمة نقل تشمل الصور» ينشئ ملف ZIP يحتوي لقطة SQLite متسقة، صور المنتجات والخلفية، وفهرسًا بإصدار الحزمة وبصمات SHA-256 لكل ملف.
- الاسترجاع يقبل `.db` القديم أو ZIP. يفحص الفهرس والبصمات والإصدار والمخطط، ويرفض الروابط الرمزية والمسارات الصاعدة وحدود الحجم قبل لمس البيانات الحية. يعيد الصور إلى مساحة التطبيق الجديدة ويحدّث مساراتها داخل القاعدة المرشحة قبل الاستبدال الآمن.
- اختبار النقل ينشئ قاعدة وصورًا في مسار مصدر ويستعيد الحزمة في مسار مختلف، ثم يثبت عودة بيانات المنتج والخلفية والملفات إلى المسار الجديد. لا تبقى البيانات مرتبطة بمسار الجهاز المصدر.
- التحقق النهائي: **114 اختبارًا ناجحًا**؛ التحليل نظيف وبناء Linux نجح. الأدلة: `reports/2026-09-11/implementation-batch-14/`.
- النسخ التلقائية السريعة كل 30 دقيقة تبقى ملفات قاعدة `.db` ولا تشمل الصور؛ حزمة النقل تُنشأ يدويًا. ما زال يلزم اختبار Windows وأجهزة حقيقية وانقطاع كهرباء فعلي.

## دفعة خامسة عشرة — إنشاء حساب المالك في أول تشغيل — 2026-09-11

- أصبح تشغيل التطبيق على قاعدة جديدة يمر بشاشة إنشاء المالك: الاسم وكلمة مرور يختارها المستخدم وتأكيدها. التشغيل الفعلي يستدعي bootstrap بلا حساب افتراضي، فلا ينشأ `owner123` في قاعدة جديدة.
- قاعدة موجودة مسبقًا لا تُعاد تهيئتها ولا تتغير حساباتها. يغطي اختبار مستقل غياب المالك، رفض كلمة المرور الافتراضية، نجاح الإعداد وتسجيل الدخول، ورفض إنشاء مالك ثانٍ؛ كما اختبر مسار الواجهة وصور Golden.
- التحقق النهائي قبل تغليف التثبيت: **115 اختبارًا ناجحًا**؛ `dart analyze lib/v2 lib/main.dart test/v2` نظيف. الأدلة: `reports/2026-09-11/implementation-batch-15/`.
- يبقى استرجاع الوصول وإثبات Windows وقبول مستخدم نهائي ضمن M4/M5.

## دفعة سادسة عشرة — حزمة Debian/Ubuntu أولية — 2026-09-11

- أضيف `tool/build_linux_deb.sh` ليحوّل Linux bundle إلى حزمة `alikhlas-pos_<version>_amd64.deb` تضم التطبيق وبياناته وقائمة التطبيقات والأيقونة تحت `/opt/alikhlas-pos`.
- تبني CI الحزمة وتفحص المحتوى وترفعها كـ artifact مستقل. بُنيت الحزمة محليًا وفُحصت بنيتها ومحتواها، لكن لم يُجر بعد تثبيت نظيف أو تشغيل فعلي على Ubuntu/Debian.
- لا يوجد حتى الآن مُثبّت Windows موقّع. لذلك I01 ما زال قيد التنفيذ ولا يمثل ذلك قبول إصدار للمستخدم النهائي.
- التحقق النهائي: `dart analyze lib/v2 lib/main.dart test/v2` نظيف، و`flutter test --no-pub` نجح بـ **115 اختبارًا**، و`flutter build linux --release --no-pub` نجح. بُنيت حزمة `alikhlas-pos_1.0.0_amd64.deb` من الناتج وفُحص ملف التحكم وملفات التطبيق وقائمة التطبيقات والأيقونة.

## دفعة سابعة عشرة — استعادة وصول المالك بلا كلمة مرور عامة — 2026-09-11

- بعد إنشاء مالك جديد، يعرض التطبيق رمز استعادة عشوائيًا مكوّنًا من خمس مجموعات؛ لا يحفظ الرمز نفسه في SQLite، بل يتحقق من قيمة PBKDF2 مشتقة منه. تُغلق شاشة الإعداد فقط بعد تأكيد المستخدم أنه حفظ الرمز.
- رابط «نسيت كلمة المرور؟» يعيد تعيين كلمة المرور من الرمز، ويبطل الرمز السابق ويعرض رمزًا بديلًا. بعد خمس محاولات خاطئة تتوقف المحاولات 15 دقيقة؛ ولا توجد كلمة مرور دعم عامة.
- المستخدمون القدامى الذين يدخلون بكلمة المرور الافتراضية يحصلون على رمز عند تغييرها الإلزامي، كي لا تنشأ قاعدة قديمة بلا وسيلة استعادة. فشل الاستعادة عند غياب رمز قديم يعرض الحد الحقيقي: اتباع إجراء الدعم المعتمد.
- اختبارات منطقية وواجهية تثبت عدم تخزين الرمز كنص، تدويره، رفض الرمز القديم، المهلة، ومسار المستخدم الحالي. يظل الرمز حماية للدخول فقط ولا يشفّر ملف قاعدة البيانات، كما يبقى إجراء الدعم والتجربة الميدانية جزءًا مفتوحًا من M4/M5.
- التحقق النهائي: `dart analyze lib/v2 lib/main.dart test/v2` نظيف، و`flutter test --no-pub` نجح بـ **119 اختبارًا**، و`flutter build linux --release --no-pub` نجح. أُعيد بناء حزمة Debian من ناتج البناء وفُحصت بنيتها ومحتوياتها، وملف CI يمر بتحليل YAML و`git diff --check` نظيف.

## دفعة ثامنة عشرة — مُثبّت Windows رسومي أولي — 2026-09-11

- أضيفت وصفة NSIS لإنشاء `ALIkhlasPOS-Setup-<version>-x64.exe` من مجلد Flutter Release. تثبت لكل مستخدم داخل `%LOCALAPPDATA%\Programs\ALIkhlas POS` وتضيف اختصار قائمة Start وسطح المكتب، وتترك بيانات التطبيق خارج مجلد التثبيت عند الإزالة.
- يثبت CI مترجم NSIS 3.12 عبر winget، ويبني المخرج ويتحقق من وجوده وحجمه ثم يرفعه باسم `alikhlas-pos-windows-installer`. يبقى bundle الخام artifact مستقلًا لأغراض التشخيص.
- تحقق YAML و`git diff --check` محليًا؛ لم يتوفر مترجم NSIS أو Windows في البيئة الحالية، لذلك لم يُبن أو يُشغّل أو يُثبت المخرج بعد. ليس له توقيع ناشر ولا يجوز تقديمه كإصدار نهائي قبل CI وتجربة تثبيت/ترقية/إزالة على Windows نظيف وتوقيع الحزمة.

## دفعة تاسعة عشرة — اسم المحل في أول تشغيل — 2026-09-11

- تتطلب شاشة المحل الجديد اسم المحل قبل إنشاء المالك، وتحفظه مع حساب المالك ورمز الاستعادة في معاملة قاعدة واحدة. لم يعد الاسم الافتراضي هو اسم إيصال محل جديد.
- تختبر الحالات رفض اسم فارغ، بقاء قاعدة بلا مالك عند الرفض، حفظ الاسم، ثم انتقال المستخدم بعد حفظ رمز الاستعادة. نجاح اختبار الواجهة يغطي كذلك الاستعادة وتسجيل الدخول اللاحق.
- لم تكتمل بعد خيارات «استرجاع نسخة سابقة» ووجهة النسخ والطابعة ضمن المعالج نفسه؛ تبقى متاحة بعد الدخول ولا تُدّعى مكتملة في إعداد أول تشغيل.

## دفعة عشرون — استرجاع محل سابق من شاشة البداية — 2026-09-11

- ظهر خيار «لدي نسخة احتياطية سابقة» قبل إنشاء المالك. يقبل قاعدة `.db` أو حزمة `.zip`، ويطلب تأكيدًا واضحًا ثم يستعمل فحص الاسترجاع ونسخة الرجوع وحاجز الكتابة الموجودة مسبقًا.
- بعد النجاح يعاد إنشاء نطاق التطبيق وتظهر شاشة الدخول لحساب النسخة المسترجعة؛ لا يطلب من المستخدم إعادة إنشاء محل فارغ. فشل اختيار ملف غير سليم يعرض رسالة عربية ويبقي شاشة الإعداد دون كتابة جديدة.
- اختبارات واجهة الإعداد ومسارات استرجاع القاعدة والحزمة المصوّرة نجحت. اختبار تفاعل منتقي ملفات النظام وقطع الطاقة أو تثبيت جهاز جديد فعلي ما زال مطلوبًا ضمن M5.
- التحقق النهائي: التحليل نظيف، وصورة Golden المحدثة لشاشة البداية اجتازت المراجعة، و`flutter test --no-pub` نجح بـ **119 اختبارًا**، وبناء Linux وحزمة Debian `alikhlas-pos_1.0.0_amd64.deb` نجحا وفُحصت محتويات الحزمة. يبقى ذلك دليلًا محليًا لا اختبار تثبيت على جهاز عميل.

## دفعة حادية وعشرون — وجهة النسخ في أول تشغيل — 2026-09-11

- صار معالج المحل الجديد يعرض اختيار مجلد للنسخ الاحتياطي كخطوة اختيارية واضحة. عند التخطي يشرح للمستخدم إمكان ضبطه لاحقًا من شاشة النسخ، فلا يمنع بدء العمل أو إنشاء حساب المالك.
- عند اختيار مجلد، يحفظ التطبيق الوجهة بعد إنشاء المالك ويحاول إنشاء أول نسخة تلقائية. تظهر للمستخدم رسالة صريحة بنجاح النسخة أو بتحذير يطلب مراجعة شاشة النسخ، بينما يبقى حسابه صالحًا للدخول في الحالتين.
- حُدّ ارتفاع بطاقة المعالج وجُعل محتواها قابلًا للتمرير، كي لا يتجاوز الشاشات الأقصر بعد إضافة الخطوة. أُضيف اختبار لتمرير المستخدم إلى زر البدء، ثم روجعت لقطة Golden الجديدة بصريًا واعتمدت.
- التحقق النهائي: التحليل `dart analyze lib/v2 lib/main.dart test/v2` نظيف؛ اختبارات المسار والنسخ اجتازت **21 حالة**، والمجموعة الكاملة `flutter test --no-pub` اجتازت **119 اختبارًا**. نجح بناء Linux، وأعيد إنشاء وفحص حزمة Debian `alikhlas-pos_1.0.0_amd64.deb`.
- لم يختبر هذا الدليل المحلي منتقي مجلدات النظام أو وسيط نسخ خارجيًا على جهاز عميل. تبقى تجربة تثبيت نظيف وترقية وإزالة على Linux وWindows، والطابعة، وقطع الطاقة، وتوقيع Windows ضمن الأعمال المفتوحة قبل الإصدار.

## دفعة ثانية وعشرون — مساعدة داخل التطبيق وتقرير دعم منقّح — 2026-09-11

- أضيف زر «المساعدة» الظاهر دائمًا في قائمة التطبيق، ويفتح شاشة عربية تشرح ما يفعله صاحب المحل قبل طلب الدعم: التحقق من الفاتورة قبل إعادة العملية، أخذ نسخة قبل الاسترجاع أو التعديل، وإعادة طباعة الفاتورة المحفوظة بدل تسجيل بيع جديد.
- تعرض الشاشة حالة النسخ بعبارات يفهمها المستخدم: هل اختيرت وجهة، ووقت آخر نسخة وعدد النسخ، مع تحذير النسخ إن وجد. لا تعرض مسار المستخدم أو اسم مجلد النسخ في تقرير الدعم.
- يستطيع المستخدم اختيار مكان حفظ تقرير نصي للدعم. التقرير يحوي نسخة التطبيق ومخطط قاعدة البيانات والنظام وحالة النسخ فقط، ويستبعد قاعدة البيانات والمبيعات والعملاء وأرقام الهواتف وكلمات المرور والمسارات المحلية. فشل نافذة الحفظ أو الكتابة يظهر رسالة عربية قابلة للتصرف بدل إيقاف التطبيق.
- اختبارات التقرير تثبت تنقيح المسارات والتحذيرات التفصيلية، واختبار الواجهة يفتح شاشة المساعدة من القائمة. روجعت لقطة الشاشة المضافة لشاشة الدعم ولقطات القائمة المتغيرة بصريًا واعتمدت.
- التحقق النهائي: `dart analyze lib/v2 lib/main.dart test/v2` نظيف، و`flutter test --no-pub` اجتاز **122 اختبارًا**، ونجح بناء Linux. أُعيد إنشاء وفحص حزمة Debian `alikhlas-pos_1.0.0_amd64.deb`، و`git diff --check` نظيف.
- لم يختبر هذا الدليل نافذة الحفظ الأصلية أو مشاركة التقرير على Windows/Linux أو وجود جهة دعم فعلية. كما يبقى دليل مصوّر وتجربة طابعة وتثبيت وترقية وإزالة على أجهزة نظيفة ضمن قبول M4/M5.

## دفعة ثالثة وعشرون — رفع متانة SQLite وقياس أثرها — 2026-09-11

- تغير إعداد SQLite مع WAL من `synchronous=NORMAL` إلى `synchronous=FULL` لتقليل نافذة فقد المعاملة المؤكدة عند تعطل النظام أو الطاقة. اختبار قاعدة ملفية يثبت أن الاتصال الفعلي يعمل بالقيمة 2.
- أضيف benchmark اختياري ينفذ خمس عمليات تشغيل حقيقية على قواعد ملفية ويعيد فتحها للتحقق. عبر 1,500 عينة لكل وضع كانت نتيجة FULL: وسيط 17.108ms وp95 يساوي 38.064ms، مقابل NORMAL: وسيط 15.286ms وp95 يساوي 56.670ms. هذه ملاحظة على جهاز التطوير وليست وعد أداء أو إثبات قطع طاقة.
- الأدلة والمنهج الكامل في `reports/2026-09-11/implementation-batch-23/`.

## دفعة رابعة وعشرون — دعم قابل للتصرف واختبار طابعة وصحة وجهة النسخ — 2026-09-11

- تعرض شاشة المساعدة إصدار الحزمة الفعلي، وتتيح معاينة تقرير الدعم المنقح قبل حفظه، وتشرح أن إرسال التقرير يدوي ولا يشمل بيانات المحل.
- أضيفت صفحة طابعة A4 عربية فعلية. روجع ناتج PDF محول إلى صورة بلا قص، مع توضيح أن قبول نظام التشغيل لطلب الطباعة لا يثبت خروج الورق.
- يعاد فحص مجلد النسخ المختار حتى قبل استحقاق نسخة جديدة؛ اختفاء الوجهة بعد نجاح سابق يظهر تحذيرًا مع إبقاء زمن النجاح، وعودتها تمسح التحذير.
- الأدلة: `reports/2026-09-11/implementation-batch-24/`. أعيد بعد آخر تعديل بناء Linux وحزمة Debian وفحص محتواها.

## دفعة خامسة وعشرون — إغلاق منافذ تكرار الحركات المالية وترحيل قاعدة آمن محليًا — 2026-09-11

- أصبحت جميع نقاط الاستخدام المالية العامة ترفض غياب مفتاح العملية قبل أي كتابة: البيع والشراء والمرتجع والمصروف، تحصيل العميل وسداد المورد، وفتح/إغلاق الوردية. تحافظ بصمة البيع كذلك على ملاحظة الدفع، مع توافق إيصالات الصيغة السابقة.
- أضيف المخزون الافتتاحي إلى نفس الضمان: يتطلب مفتاحًا عند وجود كمية، ويحفظ إيصال `opening_stock` ويدخل الطلب المالي المعلق. اختبرت إعادة الفتح، التزامن، فقد الرد، تعارض الحمولة، وفشل الإيصال مع رجوع كامل للمنتج والمخزون والقيد.
- اختبرت نقاط الواجهة الحساسة للنقرة السريعة في المصروف ونافذة الاستعادة. التحقق النهائي لجميع مسارات V2 اجتاز **143 اختبارًا** بلا فشل.
- قبل ترقية قاعدة قديمة ينشئ التطبيق لقطة SQLite محققة وبصمة وعلامة حالة. يرفض قاعدة أحدث، ويسترجع اللقطة عند اكتشاف ترحيل منقطع ثم يحجب إعادة محاولة نفس البناء. اختبارات V4→V5 وV6 والفشل المتعمد نجحت.
- الأدلة والتفاصيل: `reports/2026-09-11/implementation-batch-25/`. ما زال اختبار قاعدة بحجم إنتاج وقطع كهرباء وترقية فعلية عبر المثبت مفتوحًا.

## دفعة سادسة وعشرون — اختبار الطابعة أثناء الإعداد الأول — 2026-09-11

- أضيف اختبار طابعة اختياري قبل إنشاء المالك، يستخدم اسم المحل في صفحة A4 العربية، ويعطل عمليات الإعداد المتداخلة أثناء فتح حوار النظام.
- يمكن للمستخدم تخطي الاختبار وإكمال العمل ثم الرجوع إليه من المساعدة. الرسائل تفرق بين الإلغاء وقبول الطلب والفشل، ولا تعتبر قبول الطلب دليلًا على طباعة ورق.
- روجعت شاشة 1280x800 بصريًا، واجتاز اختبار المسار وصورة Golden والتحليل. مجموعة V2 النهائية اجتازت **143 اختبارًا**، وبناء Linux والحزمة نجحا.
- الأدلة: `reports/2026-09-11/implementation-batch-26/`. الطابعة الفعلية وWindows وتخزين طابعة مفضلة ما زالت ضمن قبول الأجهزة.

## دفعة سابعة وعشرون — مسودات فواتير قابلة للاستعادة وتحذير المغادرة — 2026-09-11

- أضيف حفظ يدوي مستقل لمسودة البيع ومسودة الشراء. تحفظ المسودة البنود والكميات والأسعار أو التكاليف وقيم الدفع والتقسيط كما أدخلها المستخدم، وتعود بعد إعادة فتح التطبيق أو تسجيل الدخول من دون إنشاء فاتورة أو قيد أو حركة مخزون.
- تحذر الواجهة قبل مغادرة فاتورة معدلة وغير محفوظة عند التنقل أو تسجيل الخروج أو إغلاق نافذة سطح المكتب. بعد حفظ المسودة يستطيع المستخدم المغادرة، كما يستطيع حذفها صراحة. المسودة التالفة لا تعطل البيع أو الشراء وتظهر رسالة عربية مع خيار المسح.
- تتحول المسودة إلى الطلب الدائم المعلق داخل معاملة واحدة قبل تنفيذ الفاتورة. يمنع التطبيق اجتماع مسودة وطلب معلق للعملية نفسها، ويمكن إعادة الطلب غير المنفذ إلى مسودة قابلة للتعديل. إذا كان إيصال العملية موجودًا يرفض الرجوع للمسودة حتى لا يعاد تنفيذ فاتورة حُفظت وفُقد ردها.
- اختبرت الواجهة عند الحد الأدنى 1024x720: تحذير المغادرة، حفظ المسودة، إعادة تشغيل النطاق، استعادة القيم الدقيقة، إدخال الأرقام العربية، التسجيل مرة واحدة، والتعامل مع مسودة تالفة. كما اختبرت قاعدة ملفية وإعادة فتحها والتحويل الذري والاسترجاع من طلب معلق.
- التحقق النهائي: `dart analyze lib/v2 lib/main.dart test/v2` نظيف، و`flutter test --no-pub test/v2` اجتاز **150 اختبارًا**، و`git diff --check` نجح. نجح بناء Linux Release، وبنيت وفُحصت حزمة `alikhlas-pos_1.0.0_amd64.deb` ببصمة SHA-256: `6a6476e0285cca9d20c2e25431548fbffb50fc20f471e078435e5cc3f65cbcc0`.
- الحفظ يدوي؛ قتل العملية أو انقطاع الكهرباء قبل ضغط «حفظ مسودة» قد يفقد التعديلات الموجودة في الذاكرة. يلزم اختبار حدث إغلاق النافذة الفعلي على Windows وLinux، ولا يغني اختبار Widget عن تجربة نظام التشغيل.
- الأدلة: `reports/2026-09-11/implementation-batch-27/`.

## دفعة ثامنة وعشرون — جرد فعلي وتسوية مخزون موثقة — 2026-09-11

- أضيف من شاشة المخزون مسار «جرد وتسوية الرصيد»: يكتب المستخدم إجمالي الكمية التي عدّها، يختار سببًا محددًا، ويراجع الرصيد السابق والكمية الفعلية والفرق وقيمة الأثر قبل الاعتماد. يتطلب «تصحيح إدخال» توضيحًا، وتقبل الكمية الأرقام العربية والإنجليزية.
- تحفظ العملية مستند `inventory_adjustments` دائمًا، وحركة مخزون، وقيد دفتر متزن داخل معاملة واحدة. النقص يحمّل حساب فروق الجرد ويخفض المخزون، والزيادة ترفع المخزون وتقيد فرقًا دائنًا؛ تظهر فروق الجرد مستقلة في ملخص اليوم والفترة وتدخل في صافي الربح.
- تعتمد القيمة على متوسط تكلفة الصنف المحفوظ. إذا لم توجد تكلفة، يطلب التطبيق تكلفة وحدة صريحة ويثبتها للصنف. يرفض التنفيذ إذا تغير الرصيد أو متوسط التكلفة منذ فتح نافذة المراجعة، حتى لا تعتمد تسوية على لقطة قديمة.
- دخلت التسوية في حماية معرّف العملية والطلب المالي المعلق. تكرار المفتاح يعيد نفس المستند، وتغيير الحمولة يرفض، وفشل كتابة الإيصال يرجع المستند والمخزون والقيد كاملًا. يمكن التحقق من الطلب بعد إعادة فتح قاعدة ملفية دون تكرار التسوية.
- رُفع مخطط Drift إلى **v6** بترحيل ينشئ جدول التسويات وفهرسه. لقطة ما قبل الترقية وآلية الرجوع الموجودة تشمل الانتقال، واختبرت ترقية v3 وv4 إلى v6 مع بقاء دليل النسخة السابقة.
- صار فحص الاتساق يرتب حركات المخزون بتسلسل المعرّفات بدل ساعة الجهاز التي قد ترجع للخلف، ويتحقق من بيانات مستند التسوية وحركته وقيده ومبالغ حسابي المخزون وفروق الجرد، ويكشف القيد المفقود دون إصلاح صامت.
- التحقق النهائي: التحليل الساكن نظيف، و`flutter test --no-pub test/v2` اجتاز **157 اختبارًا**، و`git diff --check` نجح. نجح Linux Release، وبنيت وفُحصت حزمة `alikhlas-pos_1.0.0_amd64.deb` ببصمة SHA-256: `71b8a69c23b4c009ae018f4b6f868d02fd42cbc2401e3bcb59435ec1e96b7672`.
- يظل U04 مفتوحًا للأرصدة الافتتاحية للعملاء والموردين والخزينة والمحفظة، مستند عكس التصحيح المالي، ومرتجع المشتريات. كما يلزم جرد فعلي على بيانات محل واختبار ترقية الحزمة على Windows وLinux قبل الاعتماد.
- الأدلة: `reports/2026-09-11/implementation-batch-28/`.

## دفعة تاسعة وعشرون — تعميم منع تكرار الحركات المالية — 2026-09-11

- روجعت كل نقاط الكتابة التي تنشئ قيدًا أو تغيّر رصيدًا. البيع والشراء والتحصيل وسداد المورد ومرتجع البيع والمصروف وفتح/إغلاق الوردية والمخزون الافتتاحي وتسوية الجرد تستخدم مفتاح عملية دائمًا، وتستعيد الطلب نفسه بعد فقد الرد بدل إنشاء حركة جديدة.
- أُدخل مسار الرصيد الافتتاحي الجديد في العقد نفسه: بصمة للحمولة، وإيصال في نفس المعاملة، ورفض إعادة استعمال المفتاح ببيانات مختلفة، وطلب معلّق قابل للاستعادة بعد إعادة فتح قاعدة ملفية.
- يتحقق فحص الاتساق من مستند الرصيد الافتتاحي وخطة مديونية العميل أو المورد والقيد الدفتري الدقيق، ويكشف المرجع أو الخطة اليتيمة دون إصلاح صامت. رُفع مخطط Drift إلى **v7** واختبرت ترقية v3 وv4 مع بقاء لقطة ما قبل الترقية.
- فشل حفظ إيصال الرصيد الافتتاحي يرجع المستند والخطة والقيد كاملًا. نجح التحليل، واجتازت مجموعة V2 الكاملة **159 اختبارًا**، ونجح بناء Linux Release و`git diff --check`.
- واجهة إدخال الأرصدة الافتتاحية للمستخدم النهائي لم تكتمل بعد، ومرتجع المشتريات ما زال عملية غير موجودة؛ عند تنفيذه يجب أن يدخل عقد منع التكرار قبل إتاحته للمستخدم.
- الأدلة: `reports/2026-09-11/implementation-batch-29/`.
