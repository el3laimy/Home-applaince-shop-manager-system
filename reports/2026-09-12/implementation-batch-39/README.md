# الدفعة 39: دليل البدء المرئي داخل التطبيق

- أضيفت إلى شاشة المساعدة خمسة خطوات عربية مرتبة لغير الخبير: تجهيز المحل، فتح الوردية، تسجيل البيع، متابعة الأقساط والمرتجعات، وحماية البيانات بالنسخ الاحتياطي.
- يتكيف التخطيط مع مساحة النافذة، ويعرض ثلاث بطاقات في الصف عند العرض المعتاد من دون كسر اتجاه RTL.
- أضيف وصول مباشر إلى تراخيص Flutter والحزم مفتوحة المصدر داخل التطبيق.

## التحقق

- commit محلي: `e850d5b`.
- نجح `dart analyze` للمصدر والاختبارات المستهدفة.
- نجح اختبار شاشة الدعم عند `1024x720`، ونجح اختبار الصورة الذهبية بعد مراجعة التخطيط بصريًا.

## الحدود المفتوحة

جهة الدعم وساعات الاستجابة قرار تشغيلي يحتاج بيانات معتمدة من صاحب المنتج. اختبار سهولة الخطوات مع مستخدم محل حقيقي يظل ضمن تجربة القبول.
