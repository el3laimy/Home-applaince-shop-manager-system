# دفعة 12: فحص اتساق البيانات

تم تنفيذ التحقق المحلي في 2026-09-11.

- أداة الفحص في شاشة النسخ الاحتياطي للقراءة فقط؛ لا تعدّل قاعدة البيانات.
- تغطي القيود ومراجعها، المخزون، الفواتير والمدفوعات، المرتجعات، الأقساط والذمم.
- `dart analyze lib/v2 lib/main.dart test/v2`: بلا ملاحظات.
- `flutter test --no-pub`: 112 اختبارًا ناجحًا.
- `flutter build linux --release --no-pub`: نجح؛ الناتج `Frontend/alikhlas_pos/build/linux/x64/release/bundle/alikhlas_pos`.

الدفعة تثبت الفحص وإصلاح تكوين الحركات الجديدة محليًا. لا تثبت إصلاح بيانات تاريخية، أو انقطاع كهرباء حقيقي، أو تثبيت Windows، أو طابعة مادية.
