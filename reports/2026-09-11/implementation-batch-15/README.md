# دفعة 15: إعداد أول مالك بدل كلمة مرور افتراضية

- أول قاعدة بيانات جديدة تعرض إنشاء حساب المالك بالاسم وكلمة مرور يختارها المستخدم ويؤكدها.
- تشغيل التطبيق لا ينشئ `owner123`؛ قواعد البيانات الموجودة تبقي الحسابات الموجودة فيها دون تعديل.
- اختبار منطق الإعداد واختبار واجهة البداية وصور Golden للواجهة نجحت.
- `dart analyze lib/v2 lib/main.dart test/v2`: بلا ملاحظات.
- `flutter test --no-pub`: 115 اختبارًا ناجحًا.

لا يغني ذلك عن مسار استعادة وصول موثق ومختبر أو تحقق على Windows قبل اعتماد إصدار نهائي.
