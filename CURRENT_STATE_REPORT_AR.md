# تقرير الحالة الحالية — إخلاص POS

تاريخ المراجعة: 9 سبتمبر 2026. المرجع: `db2d39f14acd4a73a6a1eb07aecf596aa548af33`، الفرع `fix/v2-installment-returns`. كانت شجرة Git نظيفة عند بدء المراجعة.

**التقييم: التطبيق يحتوي على الوظائف الأساسية لإدارة محل واحد، ويُبنى على Linux بنجاح، لكنه يحتاج إصلاحات في حماية البيانات وبعض الحالات المالية قبل اعتماده للتسليم النهائي.** نجاح البناء والاختبارات المعتادة لا يغطي المشكلات التي أثبتتها تجارب المراجعة.

## 1. حقيقة المشروع ونطاق المراجعة

- التطبيق النشط هو `Frontend/alikhlas_pos`، باستخدام Flutter Desktop وRiverpod وSQLite/drift. نقطة الدخول تشغّل `ALIkhlasV2App` مباشرة.
- لا توجد خدمة C#/.NET نشطة في مسار التشغيل الحالي؛ ملف C# منفرد وسكربت تثبيت dotnet لا يمثلان معمارية التطبيق. الكود القديم محفوظ في أرشيف يبلغ نحو 116 MB.
- التطبيق محلي وأوفلاين لمحل واحد، مع حساب المالك. لا توجد أدلة في المسار الحالي على منتج متعدد الفروع أو مزامنة سحابية؛ هذه ليست متطلبات ناقصة تلقائيًا ضمن النطاق الحالي.
- شملت المراجعة المخطط والترقيات، الدخول، البيع والشراء والمرتجعات والأقساط، التقارير، الصور، النسخ والاسترجاع، الواجهة والاختبارات وملف CI ووثائق التسليم.
- نُفذ التحليل الساكن، مجموعة الاختبارات الأصلية، بناء Linux، وخمس تجارب تشخيص بقواعد بيانات مؤقتة. لم تُستخدم بيانات المحل الفعلية ولم يتغير كود الإنتاج أو الاختبارات الأصلية.
- لم يُنفذ اختبار تفاعلي على جهاز التسليم أو Windows أو طابعة أو قارئ باركود. لم تُراجع حالة GitHub الحالية؛ معلومات artifacts في ملف التسليم تعامل هنا كتوثيق سابق فقط.

## 2. الوظائف الموجودة

| المجال | الحالة المستندة إلى الكود والاختبارات |
|---|---|
| الدخول | حساب مالك، فرض تغيير كلمة المرور الافتراضية، PBKDF2 مع salt عشوائي وترقية الهاش القديم |
| المخزون | منتجات وباركود وصور وحد أدنى للمخزون ومتوسط تكلفة وحركات مخزون |
| البيع | كاش ومحفظة وتقسيط وخصم وفاتورة وتحديث المخزون والقيد |
| المشتريات | إدخال مخزون، مورد ومدفوعات وذمم، وحساب متوسط التكلفة |
| الأقساط | جدولة وتحصيل وسداد مورد وكشف المتبقي؛ توجد مشكلة في تتابع السداد الجزئي أدناه |
| المرتجعات | ارتباط بالبيع الأصلي، كمية جزئية، تكلفة البيع التاريخية، تسوية ذمة ورد فائض؛ توجد مشكلة تقريب أدناه |
| اليومية | فتح وإغلاق وردية، حساب الفرق، مصروفات وحركات مالية |
| التقارير | دفتر محاسبي وأرصدة وكشوف حساب وتقارير فترة وتصدير PDF |
| الطباعة | PDF وخط Cairo محلي، إيصالات وملصقات باركود بأبعاد قابلة للتعديل؛ الأجهزة لم تُختبر |
| النسخ | نسخ SQLite يدوي وتلقائي عند بدء التشغيل واحتفاظ بـ30 نسخة؛ حماية الاسترجاع تحتاج إصلاحًا |
| الواجهة | عربية RTL وثيم موحد وخطوط محلية، واختبارات Widgets وGolden |

من نقاط القوة: تخزين المبالغ بوحدات نقدية صحيحة، التحقق من اتزان القيود، استخدام transactions في العمليات الأساسية، تفعيل foreign keys وWAL وفهارس، وفصل ملفات الوظائف عن الواجهات. توجد ترقيات مخطط حتى الإصدار 5. هذه عناصر جيدة، لكنها لا تثبت اكتمال سلامة كل حالة مالية أو نجاح كل ترقية لقاعدة قديمة.

## 3. نتائج التحقق الفعلية

| الفحص | النتيجة |
|---|---|
| `dart analyze lib test` داخل التطبيق | نجح: `No issues found!` |
| `flutter test --no-pub --reporter expanded` | 64 اختبارًا: **62 نجح و2 فشل**، خلال نحو 65 ثانية |
| `flutter build linux --release --no-pub` | نجح؛ الناتج `build/linux/x64/release/bundle/alikhlas_pos` |
| تجارب المراجعة | 5 من 5 أثبتت السلوك المعيب المتوقع في التجربة |
| تشغيل Windows والأجهزة الطرفية | غير منفذ في هذه المراجعة |

فشل Golden في شاشة البيع بمقدار 181 بكسل، وفي التقارير بمقدار 226 بكسل؛ كل منهما نحو 0.02%. تمت معاينة صور الفروق: الاختلافات في نصوص التواريخ. الكود يستخدم `DateTime.now()` في تاريخ القسط وفترة التقرير، والاختبار لا يثبت الساعة. النتيجة تشير إلى اختبارات بصرية تعتمد على تاريخ التنفيذ؛ لا ينبغي استبدال الصور المرجعية تلقائيًا باعتبار ذلك إصلاحًا للتصميم.

تطلب تشغيل Flutter السماح له بتحديث الكاش خارج مساحة المشروع، ثم نجح البناء واكتملت الاختبارات. نتائج الاختبارات هنا تخص البيئة المحلية، ولا تُستنتج منها نتائج Windows.

## 4. المشكلات مرتبة بالأولوية

### F1 — P1: الاسترجاع يقبل ملفًا تالفًا ويزيل نسخة الرجوع

المصدر: `Frontend/alikhlas_pos/lib/v2/application/use_cases/v2_backup_settings_use_cases.dart:35`، وبالأخص السطور 60–74.

الدالة تتحقق من وجود الملف فقط، ثم تغلق القاعدة وتنسخ الملف فوقها. نجاح النسخ يعتبر نجاحًا للاسترجاع وتُحذف نسخة rollback، دون فتح الملف والتحقق من سلامته أو توافق مخططه. اختيار امتداد `.db` من الواجهة لا يثبت صحة المحتوى.

**الإثبات:** استرجاع ملف `.db` يحتوي نصًا عاديًا انتهى دون خطأ، وأصبح ملف القاعدة يحمل النص نفسه، ولم تبقَ نسخة `.bak`. حماية rollback الموجودة تعمل عند فشل عملية النسخ، لا عند فساد الملف المنسوخ.

الأثر: فقد قاعدة التشغيل الحالية إذا لم توجد نسخة مستقلة صالحة. المطلوب: فحص ملف مرشح منفصل وسلامة SQLite والمخطط قبل الاستبدال، ثم استبدال آمن مع إبقاء نسخة الرجوع حتى نجاح فتح القاعدة المسترجعة. كذلك يلزم منع استمرار الواجهة في استخدام اتصال مغلق بعد الاسترجاع.

### F2 — P1: تعطل مجلد النسخ الاحتياطي قد يمنع فتح التطبيق

المصادر: `v2_auth_shift_use_cases.dart:25`، `v2_backup_settings_use_cases.dart:22`، و`lib/v2/app/v2_app.dart:65` داخل المسار النشط.

`bootstrap()` ينتظر النسخ التلقائي دون معالجة فشله. خطأ الوصول للمجلد ينتقل إلى شاشة الخطأ العامة قبل الدخول، ولا توجد في هذا المسار فرصة للوصول إلى إعدادات النسخ لإصلاح المسار.

**الإثبات:** حفظ مسار نسخ لا يمكن إنشاؤه ثم تنفيذ bootstrap أدى إلى `FileSystemException` رغم أن قاعدة التشغيل سليمة. يمكن أن تظهر المشكلة مع وسيط نسخ غير متاح أو تغير الصلاحيات.

المطلوب: إظهار فشل النسخ كتنبيه قابل للإصلاح مع السماح بفتح قاعدة التشغيل السليمة، وفصل سلامة قاعدة التشغيل عن توفر وجهة النسخ.

### F3 — P2: تتابع الدفعات الجزئية يترك القسط المسدد بحالة غير صحيحة

المصادر: `v2_installment_use_cases.dart:187` و`v2_report_helpers.dart:117` داخل مجلد `application/use_cases`.

خوارزمية توزيع الدفعة تقارن الدفعة الجديدة بكامل مبلغ القسط، ولا تخصم ما سبق دفعه جزئيًا لذلك القسط.

**الإثبات:** خطة بها قسطان، كل منهما 100 جنيه؛ تحصيل 40 ثم 60 جنيهًا جعل إجمالي المحصل 100 جنيه صحيحًا، لكن القسط الأول بقي `partial` واستمر في قائمة المستحقات. الأرصدة الإجمالية في التجربة صحيحة؛ المشكلة في حالة الجدول وتنبيهات الاستحقاق واتساق العرض.

المطلوب: توزيع المجموع المحصل تراكميًا أو حفظ المدفوع لكل قسط، وتوحيد مصدر حالة الجدول والقائمة اليومية. يضاف اختبار دفعات متتالية تعبر حدود أكثر من قسط.

### F4 — P2: تكرار الصنف داخل مدخلات الفاتورة يفسد تحديث الكمية

المصدر: `v2_sales_purchase_return_use_cases.dart:40` و`:107` و`:259`.

يجري حفظ نسخة واحدة من رصيد المنتج قبل التحديث ثم إعادة استخدامها لكل سطر، دون تجميع كميات الصنف المتكرر.

**الإثبات في البيع:** رصيد 5 قطع، وسطران من الصنف نفسه بكمية 3 لكل سطر؛ قُبل بيع 6 قطع وانتهى الرصيد إلى 2، بدل رفض البيع. الشراء يحتوي النمط نفسه، لكنه لم يخضع لتجربة مستقلة في هذه المراجعة.

قيد مهم: واجهتا البيع والشراء تجمعان الصنف المتكرر حاليًا؛ لذلك ثبت العيب عند حد use-case، ولم يثبت الوصول إليه من السلة العادية. المطلوب رفض التكرار أو تجميعه داخل طبقة العمليات نفسها قبل فحص الرصيد وحساب التكلفة.

### F5 — P2: تقريب الخصم لكل وحدة قد يزيد أو ينقص رد المرتجع

المصدر: `v2_sales_purchase_return_use_cases.dart:724` واستخدام الناتج عند السطر 412.

تقريب صافي سعر الوحدة ثم ضربه في الكمية لا يضمن أن مجموع الرد يساوي صافي الفاتورة.

**الإثبات:** بيع 3 قطع بسعر 100 جنيه للقطعة، وخصم إجمالي قرش واحد؛ الصافي 299.99 جنيه، بينما المرتجع الكامل حُسب 300.00 جنيه. التجربة سمحت بتجاوز الرصيد السالب لقياس مبلغ الرد؛ دون ذلك قد يظهر طلب تأكيد إذا لم يوجد رصيد آخر بالمحفظة.

المطلوب: توزيع الخصم وفروق التقريب بوحدات نقدية صحيحة على السطور والكميات، مع احتساب ما رُد سابقًا بحيث لا يتجاوز مجموع المرتجعات صافي قيمة البضاعة.

### F6 — P2: النسخة الاحتياطية لا تنقل صور المنتجات والخلفيات

المصادر: `v2_backup_settings_use_cases.dart:4` و`lib/v2/app/local_image_store.dart:45`.

النسخ ينشئ ملف SQLite فقط، بينما الصور محفوظة في مجلدات منفصلة وتخزن القاعدة مساراتها المحلية. استعادة `.db` على جهاز جديد لا تستعيد ملفات الصور أو تصحح مساراتها.

هذه نتيجة مراجعة الكود، وليست تجربة انتقال جهاز. المطلوب تعريف واضح لنطاق النسخة، ويفضل حزمة بيانات وصور مع مسارات قابلة للنقل وفحص اكتمال الاستعادة.

### F7 — P2: CI لا يشغّل الاختبارات قبل إنتاج حزم التسليم

المصدر: `.github/workflows/windows-build.yml`.

وظيفة Windows تشغّل التحليل والبناء؛ Linux تشغّل البناء فقط. لا توجد خطوة `flutter test`، ولا trigger لـ`pull_request` في workflow الحالي؛ التشغيل التلقائي مخصص لفرع `fix/v2-installment-returns` ومسارات محددة.

الأثر: يمكن إنتاج artifact ناجح مع اختبارات فاشلة أو تراجع في الحسابات. المطلوب تثبيت الوقت في Golden، ثم جعل التحليل والاختبارات بوابة للحزم ولطلبات الدمج على الفروع المعتمدة.

## 5. الصيانة والتوثيق وحدود الضمان

- ملف التسليم يشير إلى artifact من `f69f814`، بينما HEAD الحالي `db2d39f` يتضمن تغيير أيقونة لاحقًا. لا يصح وصف artifact القديم بأنه نفس الشجرة الحالية. بناء Linux في هذه المراجعة يخص HEAD الحالي.
- `README.md` يقول إن إثبات Windows مطلوب، بينما ملف التسليم يسجل نجاح CI سابقًا. يلزم التفريق في الوثائق بين نجاح البناء السابق ونجاح تشغيله على الجهاز المستهدف، وتحديث المرجع عند الإصدار المقبل.
- اسم المنتج التسويقي v2 لكن `pubspec.yaml` ما زال `1.0.0+1` ووصفه قالب Flutter. يفضل توحيد ترقيم الإصدارات وبيانات الحزمة قبل التوزيع.
- `workbenchSnapshot()` يحمل جميع المنتجات والعملاء والموردين وخطط مفتوحة، وتتشارك الوحدات مكتبات كبيرة عبر `part`. هذا يزيد تكلفة التوسع والصيانة؛ لا توجد قياسات تثبت مشكلة أداء فعلية الآن. يلزم قياس ببيانات واقعية قبل تحسينات واسعة.
- لم تُحسب نسبة تغطية رقمية، ولم يُثبت اختبار ترقية فعلي من كل إصدار مخطط سابق. عدد الاختبارات الناجحة ليس نسبة اكتمال للمنتج.
- فائدة التقسيط لا تُرد تلقائيًا حسب السياسة الموثقة؛ هذه حدود وظيفة معلنة وليست خطأ أضيف ضمن النتائج.
- قاعدة البيانات والنسخ محلية؛ تسجيل الدخول وحده لا يثبت حماية ملفات البيانات على مستوى نظام التشغيل. لم تتضمن المراجعة تدقيق صلاحيات جهاز العميل أو تشفير القرص أو فحصًا خارجيًا لثغرات الاعتماديات.

## 6. ترتيب العمل المقترح ومعايير القبول

1. **حماية البيانات والتشغيل:** إصلاح F1 وF2، ثم تجربة ملف تالف ومخطط غير متوافق ووجهة نسخ غير متاحة وفشل الاستبدال. النجاح: تبقى القاعدة الأصلية قابلة للاستخدام ويمكن إصلاح إعدادات النسخ من التطبيق.
2. **سلامة الحالات المالية:** إصلاح F3 وF4 وF5 باختبارات انحدار للسلوك الصحيح، والتحقق من المخزون والذمم والمستحقات ومجموع الرد بعد دفعات ومرتجعات متتابعة.
3. **بوابة إصدار موثوقة:** تثبيت الوقت في Golden وإدخال الاختبارات في CI، ثم إنشاء حزم تشير إلى commit واحد محدد وتحديث وثائق التسليم. النجاح: التحليل والاختبارات والبناء تمر جميعها.
4. **تسليم فعلي:** يوم تشغيل كامل على قاعدة اختبار بجهاز العميل، مع Windows عند استهدافه، وإيصال وملصق باركود ومسح فعلي، ونسخ واسترجاع وإعادة تشغيل. تشمل تجربة النقل الصور إذا اعتُمدت ضمن النسخة.

القرار الحالي: مناسب لاستكمال التطوير والتجربة المنضبطة؛ لا أوصي باعتباره تسليمًا نهائيًا قبل إغلاق مخاطر فقد البيانات والحالات المالية المثبتة واجتياز اختبار الجهاز.

## 7. الأدلة المحفوظة

داخل `reports/2026-09-09/`:

- `flutter-test.log`: نتيجة الاختبارات الأصلية 62 ناجحًا و2 فاشلان.
- `linux-build.log`: نجاح بناء Linux.
- `audit-probes.log`: نتائج تجارب إثبات العيوب الخمسة.
- `audit_probes_test.dart`: مصدر التجارب. **تأكيداته تصف السلوك المعيب المرصود؛ نجاحه يثبت وجود العيب ولا يعني صحة التطبيق.** حُفظ خارج مجموعة اختبارات المنتج حتى لا يصبح اعتمادًا لهذا السلوك.
- `pos-diff.png` و`reports-diff.png`: صور الفروق البصرية التي جرت معاينتها.

نُفذت التجارب مؤقتًا من `test/v2/current_state_audit_probe_test.dart` داخل التطبيق بالأمر `flutter test --no-pub test/v2/current_state_audit_probe_test.dart --reporter expanded`، ثم نُقل مصدرها إلى مجلد الأدلة. تستخدم SQLite بالذاكرة أو مجلدات مؤقتة تُحذف بعد الاختبار. لا تتضمن هذه المراجعة إصلاحات إنتاجية.
